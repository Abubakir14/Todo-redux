const initialState = {
  Checkbox: false
}





// fewgtrbft
const checked = (state = initialState, action) => {
    if(action.type === 'CHECKBOX') {
        return {
            ...state
            
        }
    }
}